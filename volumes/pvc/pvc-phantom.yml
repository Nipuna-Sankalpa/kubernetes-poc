apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: web-directory-phantom
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeName: pv-name-phantom